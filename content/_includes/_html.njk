<!DOCTYPE html>
<html lang="en-US">
<head>
	<meta charset="UTF-8">
	<title>{{ title }}</title>

  {% if excerpt %}
  <meta name="description" content="{{ post.data.excerpt }}">
  {% endif %}

  <link rel="stylesheet" href="/_css/main.css" type="text/css" media="all">

</head>
  <body id="top">

    <section>
      <header>
      	<p><a href="/">My Site</a></p>
				<p><a href="/archives/all/">All Posts</a></p>
				<p><a href="/archives/tags/">All Tags</a></p>
      </header>

      <div class="content">
				{% block content %}{% endblock %}

				{% if layout in [ 'post'] %}

					<li>
						<p><strong>Published:</strong></p>
						<p>{{ page.date | dateformat }}</p>
					</li>

					{% if page.modified %}
					<li>
						<p><strong>Modified:</strong></p>
						<p>{{ page.modified | dateformat }}</p>
					</li>
					{% endif %}

					{% if tags | length %}
					<li>
						<p><strong>Tags:</strong></p>
						<p>
							{%- for tag in tags -%}
								<a href="/tag/{{ tag | slug }}">{{ tag }}</a><br>
							{%- endfor -%}
						</p>
					</li>
					{% endif %}

				{% endif %}
    	</div>
    </section>

    <footer>
				<p>Built with <strong>Eleventy</strong></p>
  	</footer>
  </body>
</html>
